# Project Log — 2026-06-07

> Auto-maintained progress log. Newest entries at the bottom.

---

### 00:30 — CUDA graph Phase 1: kernel stream correctness (done + validated)
- Fixed root cause from Phase 0: all 7 hand-written kernel launches ran on the default stream, so `torch.cuda.graph` recorded nothing
- Added `, 0, at::cuda::getCurrentCUDAStream()` to launches in `embedding/rmsnorm/residual_ops/swiglu/attention` kernels; added `<ATen/cuda/CUDAContext.h>` include to embedding + rmsnorm; rebuilt all exts (`build_kernels.sh all`)
- Re-ran `phase0_graph_diag.py`: "empty graph" warning gone; `replay == eager` exactly at every stage (op→layer→stack→full). `phase0_graph_probe.py`: replay vs eager max|Δ| = **0.0000** → PASS
- Eager parity intact: `test_executor.TestExecutorGpu` **4/4 OK**
- Confirmed allocation hypothesis is dead (caching-allocator intermediates replay bit-exact); no per-op `_out` rewrite needed
- New concept documented: capture **records but does not execute** — output buffer is zeros until first `replay()`; fixed the probe's PASS logic accordingly
- Wrote `documentation/cuda_graph_issues_and_concepts.md` (Issue #1 = stream bug, full concept writeup); updated `graph_plan.md` + `cuda_graphs_explained.md`
- **Next:** Phase 2 — device-scalar `write_pos`/`cur_len` so a graph can drive real multi-step decoding (positions currently baked at capture time)

### 01:30 — CUDA graph Phase 2: device-scalar position kernels (done + validated)
- Added `_dev` attention ops reading `write_pos`/`cur_len` from a 0-d int64 CUDA scalar: `rope_kv_write_forward_dev`, `decode_attn_forward_dev`, `small_q_attn_forward_dev` (kernel.cu + bindings.cpp + ops.py + __init__.py)
- Design: optional `const int64_t*` override param; null → host int (eager path untouched), non-null → read device scalar in-kernel. One launcher/dispatch for both paths (no body/dispatch duplication)
- Refactored `decode_attn_check` to shape-only; cur_len range check moved inline to host-int forwards (the `_dev` path can't host-check without a sync)
- `test_attention_dev_scalar.py`: 5/5, dev == host **bit-exact** (`torch.equal`) across positions for decode/small_q/rope_kv_write (±RoPE) + bad-scalar rejection. Regression `test_attention` 8/8 OK
- Build gotcha: attention `kernel.cu` OOM-kills `cicc` (signal 9) on login node → build via `srun --mem=32G`
- Wrote Concept #2 in `documentation/cuda_graph_issues_and_concepts.md` (frozen scalars → device memory; host value-checks move to caller; S stays a host shape)
- **Next:** Phase 3 — static buffers + allocation-stable `_decode_forward_static()` using `_dev` ops

### 02:30 — CUDA graph Phase 3: static decode forward (done + validated)
- `RuntimeBuffers`: added `static_input_ids`/`static_cur_len`/`static_cos`/`static_sin` + `refresh_decode_rope(pos)`; kept out of `plan_memory` seq-scaling (sub-KB fixed scratch — folding in would break the linear-scaling test)
- `executor.py`: `static_decode` branch in `_run_attention` (uses `_dev` ops + `cache_position`/`static_cur_len` scalars + static cos/sin), threaded through `_run_decoder_layer`/`_forward_stack`; new `_prepare_decode_static` / `_decode_forward_static` / `decode_step_static`
- `test_decode_static.py`: static path == eager `decode_step` **bit-exact** (`torch.equal`) over 6-step greedy trajectory; 2/2 OK
- Test-isolation gotcha: `test_decode_static` holds a 7B (`setUpClass`) → don't co-run with `test_buffers` GPU (loads 2nd 7B) → OOM; run separately
- Wrote Concept #3 (prepare/forward split = capture/replay boundary) in `cuda_graph_issues_and_concepts.md`
- Added user-requested future opt to `graph_plan.md`: fold RoPE slice+update into the graph via device-scalar table indexing (memory-free)
- **Next:** Phase 4 — capture `_decode_forward_static` into a CUDAGraph + `decode_step_graph` replay loop

### 03:30 — CUDA graph Phase 4+5: capture/replay works; 7B decode is memory-bound (1.00x)
- `executor.py`: `decode_step_graph` + `_capture_decode_graph` (side-stream warmup, shared pool, lazy one-time capture, one graph reused across prompts); `use_cuda_graph` flag
- `test_decode_graph.py`: graphed decode == eager **bit-exact** over 8-step trajectory + reused across prefills; 2/2 OK
- `runtime/benchmarks/phase4_graph_decode.py`: eager **33.6** vs graph **33.6 tok/s** → **1.00x speedup**
- **KEY FINDING:** 7B batch-1 decode is **memory-bandwidth-bound**, not launch-bound. GPU floor ≈ 22ms/tok (15GB weights / 672 GB/s); measured 29.7ms. Host dispatch drops 28.1→20.6 ms/tok with the graph but is already hidden behind GPU work → no wall-clock win
- Graph machinery is correct + reusable; real payoff is the **0.5B draft** (Phase 8d, ~14x less weight traffic), larger batch, or quantized weights. 7B VERIFY graph would also be ~1.00x
- Wrote Concept #4 (launch-bound vs memory-bound; measure before optimizing); reprioritized Phase 5.5/6 in `graph_plan.md`
- **Decision point for user:** where to point the working graph machinery next

### 10:55 — Sync `runtime/` from `origin/eli_dev` into `draft_model_files/`
- `git fetch origin eli_dev` + `git archive origin/eli_dev runtime | tar -x -C draft_model_files`
- 81 files at `draft_model_files/runtime/` (draft + target kernels, executor, tests, configs)

### 11:30 — CUDA graph Phase 5.5+6+7: target-model graphs COMPLETE
- Phase 5.5: `greedy_extend(use_cuda_graph=…)` → routes to `decode_step_graph`
- Phase 6: `verify_gamma_graph` — one graph per query length S (= γ or γ+1); `test_verify_graph` 3/3 **bit-exact** vs eager (no-bonus, leading-bonus, per-γ caching)
- Refactor: unified static attention path (`static_attn` flag + `self._static_ctx`; S=1→decode_attn_dev, S>1→small_q_dev), per-S verify static buffers (lazy, contiguous), shared graph pool. Regressions green: `test_decode_graph` 2/2, `test_attention_dev_scalar` 5/5
- Wrote `documentation/target_graph_benchmarks.md` (standalone benchmark record: decode 1.00×, verify ~1.00×, memory-bandwidth analysis)
- **Target graphs are an available option** (behind `use_cuda_graph` / `decode_step_graph` / `verify_gamma_graph`), not a 7B speedup (memory-bound)
- **Next:** apply same pipeline to 0.5B draft (launch-bound → real win); draft kernels in `draft_model_files/`

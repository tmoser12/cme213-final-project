# Project Log — 2026-06-05

> Auto-maintained progress log. Newest entries at the bottom.

---

### 14:46 — Agent skills infrastructure
- Set up `.cursor/skills/` directory for project-specific agent skills
- Created `gpu-cluster` skill: SLURM semantics, setup.sh requirement, gpu-turing partition limits (5 nodes, 20 GPUs, 30-min cap), Quadro RTX 6000 specs
- Added `slurm/run_python.sh` wrapper for general Python-on-GPU execution (scripts, modules, CLI args)
- Expanded skill with interactive shell workflow, manual srun examples, and batch job template

### 14:46 — Progress logging skill
- Created `progress-logging` skill for passive daily logs in `project_logs/`
- Seeded today's log file with session work so far

### 14:47 — Progress logging always-on rule
- Added `.cursor/rules/progress-logging.mdc` with `alwaysApply: true`
- Enforces passive log updates at end of every substantive turn

### 14:54 — Phase 1 runtime contract (plan.md)
- Created `runtime/` module layout: `contract/`, `config/`, `weights/`, `memory/`, `ops/`, `executor/`
- Froze V1 dtype policy (FP16 data, FP32 accum), tensor descriptors, canonical Qwen2 shapes, op ABI (`init`/`workspace_bytes`/`run`)
- Skeleton `WeightManager`, `MemoryPlanner`, `OpDispatch`, `DecoderExecutor`, `Qwen2Executor` with mock ops
- Added 17 tests in `tests/` — all passing via `python -m unittest discover -s tests -v`

### 15:05 — Simplified YAML-driven runtime
- Removed over-engineered packages (contract/, weights/, ops/, executor/ enums and mock dispatch)
- Added `runtime/configs/qwen2.5-7b.yaml` and `qwen2.5-0.5b.yaml` as single source of truth per model
- Replaced with 3 modules: `config.py`, `shapes.py`, `memory.py` — same API works for both models via YAML path
- Rewrote tests (10 passing), updated `plan.md` and `requirements.txt` (pyyaml)
- Deleted leftover empty dirs: `config/`, `contract/`, `executor/`, `memory/`, `ops/`, `weights/`, `production_kernels/`, `tests/`

### 15:17 — Phase 2 weight loading
- Added `runtime/weights.py`: `load_weights()`, `validate_weights()`, `expected_keys()`, `startup_report()`
- Loads sharded (7B) or single-file (0.5B) safetensors from YAML `model_path`, casts to FP16, validates all HF keys/shapes incl. q/k/v biases
- Fixed `shapes.weight_shapes()` to HF `[out, in]` layout (e.g. k_proj is `(512, 3584)` not transposed)
- Added `tests/test_weights.py` — 0.5B + 7B load tests pass on CPU; GPU test skips on login node

### 15:21 — GPU HBM loading + VRAM budget
- `load_weights_on_gpu()`, `gpu_memory_snapshot()`, `vram_budget()` — weights transferred to GPU HBM one tensor at a time
- `max_seq_len_after_weights()` / `max_seq_len_for_budget()` in `memory.py` — linear seq scaling, 512 MiB reserve
- GPU tests require Turing node: `bash slurm/run_tests_gpu.sh` — 0.5B and 7B load + max_seq_len checks pass

### 15:25 — Single-model VRAM budget (7B canonical)
- Clarified: one model per GPU only; budget is free HBM after that model's weights (production: 7B)
- `load_weights_on_gpu()` clears CUDA cache before load; budget includes `single_model_on_gpu` flag
- Canonical math test uses 7B on 24GB; added test that reserved VRAM ≈ 7B weights alone (not both models)
- `slurm/run_tests_gpu.sh` defaults to `TestGpuLoad7B`

### 15:26 — Moved tests into runtime/
- Relocated `tests/` → `runtime/tests/` (test_config, test_shapes, test_weights)
- Updated README, plan.md, `slurm/run_tests_gpu.sh` module paths (`runtime.tests.*`)
- Run all tests: `python -m unittest discover -s runtime/tests -t . -v`

### 15:32 — 7B VRAM budget in README
- Measured on Turing GPU: 14,526 MiB weights, 7,984 MiB free, max seq len 18,525 @ batch=1
- Added table + `runtime/tests/print_7b_vram.py` re-measure command to `runtime/README.md`

### 15:40 — Phase 3 RMSNorm production kernel
- Replaced monkey-patch `wrapper.py` with `ops.py` host ABI: `init()`, `workspace_bytes()`, `forward(input, weight, eps)`
- Added `runtime/tests/test_rmsnorm.py` — HF parity + real 7B safetensor weights (4 tests pass on GPU)
- Full decoder integration deferred until remaining kernels arrive

### 15:55 — Strip bench/eval from production_kernels
- Removed `benchmark.py` and `run_benchmark.sh` from `production_kernels/rmsnorm/` — inference-only code remains
- All testing via `runtime/tests/` (e.g. `test_rmsnorm.py`)

### 16:10 — Reorganize runtime setup code into core/
- Moved `config.py`, `shapes.py`, `memory.py`, `weights.py` and YAML configs into `runtime/core/`
- Updated imports in tests, `runtime/__init__.py` (re-exports), `README.md`, `plan.md`
- CPU tests pass (26 tests, 9 GPU skipped on login node)

### 16:25 — GPU RMSNorm tests (7B)
- Ran `runtime.tests.test_rmsnorm.TestRmsnormGpu` on `gpu-turing` — 3/3 OK in 8.8s
- 7B HF parity (batch/seq sweep) and real layer-0 `input_layernorm` weights both match within atol=1e-3

### 16:48 — Revert uncommitted C++ refactor
- `git reset --hard HEAD` + `git clean -fd` to discard WIP (runtime/host, production_kernels/target, etc.)
- Manually removed leftover `runtime/host/build/` artifacts (nested git repos skipped by clean)
- Working tree clean at `6a969c2` (phase 1-3 python)

### 16:53 — Replace runtime/plan.md (Python host pivot)
- Rewrote `runtime/plan.md` — Python host + C++/CUDA kernels via `ops.py` JIT pybind
- Phases 1–2 marked done; Phase 3–7 updated (restore rmsnorm, promote target/ kernels, buffers, executor)
- Explicitly abandoned C++ inference host (`runtime/host/`, CMake/LibTorch)

### 16:55 — Phase 3 scope: all target kernels ready
- Updated `runtime/plan.md` — partner delivered all five ops in `production_kernels/target/`
- Merged RMSNorm-first gating into single Phase 3: promote all ops + parity tests in one pass
- Phase 5 executor unblocked once full Phase 3 promotion completes

### 16:59 — Phase 3: AOT build + target/draft kernel layout
- Replaced JIT (`jit.py` at import) with AOT compile via `setup.py` + `scripts/build_kernels.sh`
- Kernels stay in `production_kernels/target/` (no promotion); `draft/` reserved for speculative model
- Added `kernel_set` YAML field; ops.py imports prebuilt extensions directly

### 17:07 — Expert perf feedback integrated into plan.md
- Phase 3: bindings audit (const ref tensors, GIL release); Phase 1: Turing dim alignment checks
- Phase 4/5: device-side cache_position, zero-sync rule, 16-byte KV strides
- Phase 7: CUDA Graphs for decode; optional fuse RoPE+kv_write at end

### 19:33 — Remove optional RoPE+KV fusion from plan
- Partner already implemented fusion in target attention kernels
- Dropped `fuse-rope-kv-write` todo and optional fusion section from `runtime/plan.md`

### 19:34 — Re-sync production_kernels from eli_dev
- Fetched updated `origin/eli_dev` (c9d6912 → 40ff13c); replaced `runtime/production_kernels/` via `git archive`
- 38 files: attention, embedding, residual_ops, rmsnorm, swiglu (flat layout, no `target/` subdir)

### 19:54 — Runtime kernel system documentation
- Added `documentation/runtime_kernel_system.md`: AOT build, ops.py ABI, per-op APIs, executor wiring (planned)
- Covers layer stack (kernel.cu → bindings → extension → ops.py), testing pattern, JIT legacy path

### 19:55 — Kernel system doc: concrete code snippets
- Expanded `documentation/runtime_kernel_system.md` with real excerpts from kernel.cu, bindings.cpp, ops.py, setup.py, tests, wrapper.py
- Added end-to-end call trace, executor pseudocode, JIT vs AOT comparison table

### 20:00 — Phase 3 complete: AOT kernels + ops.py + GPU tests
- Added `setup.py`, `scripts/build_kernels.sh` (NVHPC sanitize, gnu12, per-kernel BUILD_KERNEL)
- Integrated all five target ops: rmsnorm, embedding, residual_ops, swiglu, attention — each with `ops.py`, GIL-release bindings, AOT import test
- GPU parity: 25/25 tests pass (`runtime.tests.test_*` for all five ops on gpu-turing)
- `.gitignore`: `build/`, `target_*_ops*.so`; updated `runtime/plan.md` Phase 3 todos to completed

### 20:01 — AOT .so files colocated with kernels
- `setup.py` uses dotted module paths (`runtime.production_kernels.target.<op>.target_<op>_ops`)
- `build_ext --inplace` copies `.so` into each `target/<op>/` dir next to `ops.py`
- Updated ops.py (relative imports), tests (path check), `.gitignore`; 25/25 GPU tests still pass

### 20:02 — Remove deprecated JIT-era kernel files
- Deleted per-op `jit.py`, `wrapper.py`, `benchmark.py`, `run_benchmark.sh` under `target/`
- Deleted `attention/benchmark_scripts/`, `swiglu/_correctness_check.py`
- Updated `runtime/README.md` and `runtime/plan.md` — AOT + `runtime/tests/` only

### 20:26 — Phase 4 buffer allocation complete
- Added `runtime/buffers.py` (`RuntimeBuffers`, `allocate_buffers`, RoPE tables, ping-pong swap, KV layer views)
- Extended `plan_memory()` / `shapes.py` for rope_cos/sin + fixed cache_position bytes
- `runtime/tests/test_buffers.py`: 10/10 pass on gpu-turing (shapes, RoPE vs HF, VRAM budget after weights)
- Fixed `test_runtime_scales_linearly_with_seq` — account for one-time cache_position overhead
- Marked Phase 4 completed in `runtime/plan.md`

### 21:15 — Phase 5 executor complete (gpu-turing time-limit fixes)
- Added `runtime/executor.py` (`Qwen2Executor`, `prefill`, `decode_step`, layer_order loop, QKV stack)
- `runtime/tests/test_executor.py`: 5/5 pass — single HF model load (avoids 24GB OOM), ~14s GPU run
- Fixes: `cuda` vs `cuda:0` device check; HF logits `.float()` for allclose vs fp16 kernels
- Prior 30 min timeout: duplicate HF loads + `--time=00:45:00` (partition max is 30 min)
- Marked Phase 5 completed in `runtime/plan.md`

### 21:20 — GPU skill updated with runtime findings
- `.cursor/skills/gpu-cluster/SKILL.md`: 30 min cap behavior, `run_tests_gpu.sh`, VRAM one-copy rule, parity pitfalls table, checklist items

### 21:25 — plan.md synced to Phases 1–5 progress
- Replaced stale "Current Gap" with status table (30/30 GPU tests, phase checklist)
- Phase 3 marked complete (3c kernel_set pending); Phase 6 partial progress noted
- Updated usage example, layout labels, immediate next actions → Phase 6/README

### 21:45 — Phase 6 parity harness complete
- Added `runtime/tests/parity_support.py`, `test_decoder_layer.py` (4 layer tests), `test_parity_greedy.py` (8+16 token trajectories)
- `executor.run_decoder_layer()`, `executor.greedy_extend()`; fixed greedy loop (prefill argmax before decode_step)
- All Phase 6 GPU tests pass on gpu-turing; plan.md + `run_tests_gpu.sh` updated

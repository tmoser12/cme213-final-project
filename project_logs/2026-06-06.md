# Project Log — 2026-06-06

> Auto-maintained progress log. Newest entries at the bottom.

---

### 12:04 — Runtime architecture documentation
- Wrote `documentation/runtime_architecture_and_execution.md` — full runtime/ structure, forward-pass sequence, buffers/KV cache, prefill vs decode, sampling
- Read executor, buffers, core/, attention kernel, parity tests for accuracy
- Outcome: comprehensive execution walkthrough in documentation dir

### 15:40 — Phase 8 plan: ForwardMode + host sampling / graphs
- Updated `runtime/plan.md`: explicit PREFILL / VERIFY / DECODE stages (verify ≠ decode S=1)
- Host sampling rationale: D2H for local target sampler; draft q via MPI, not target p
- CUDA graph priorities: verify primary, commit decode secondary, prefill optional; never graph sampler/MPI

### 22:50 — Phase 8a implemented (no MPI)
- `ForwardMode`, `verify_gamma`, `rollback_cache`, `p1_logits` in `runtime/executor.py`
- `runtime/speculative/{types,sampler,target_step}.py`; `target_speculative_step()` with dummy draft logits
- Tests: `test_speculative_sampler.py` (5 CPU), `test_speculative_target.py` (5 GPU + no-mpi check)
- All Phase 8a GPU tests pass; existing executor parity unchanged

### 16:04 — test_mpi mock spec-decode prototype
- Added `test_mpi/` — 2-rank mpi4py loop mimicking draft→target wire protocol (no models)
- `protocol.py`: fp16 logits sent as `uint8` view (direct float16 Send/Recv segfaults on NVHPC OMPI)
- `sampler.py`: NumPy-only accept/reject (avoids torch+MPI on login node)
- Verified: unit tests (5), local `mpirun -np 2`, SLURM CPU via `bash test_mpi/run_mpi.sh --slurm-cpu`
- Installed `mpi4py>=4.0.0`; SLURM pattern from cme213-hw7: one task + `mpirun --oversubscribe -np 2`

### 16:06 — test_mpi README for Phase 8c port
- Added `test_mpi/README.md`: wire protocol, cluster quirks, SLURM launch, port checklist → `runtime/speculative/`

### 16:12 — test_mpi 2-GPU verification
- `test_mpi/gpu_check.py`: bind rank→`cuda:{rank}`, fp32 probe kernel, `nvidia-smi` UUID allgather
- `--require-gpu` flag; auto-enabled by `run_mpi.sh --slurm-gpu`; verified on hpcc-gpu-5-1 (distinct GPU UUIDs)

### 20:15 — Bundled bonus verify (single target forward)
- `verify_gamma(..., commit_bonus=)` prepends deferred bonus to γ drafts (S=γ+1, one VERIFY)
- Removed separate S=1 commit before verify; `flush_pending_bonus()` only after final iter
- MPI `TargetResult` adds `cache_pos_after`; draft sync uses bundled-bonus detection
- Updated plan.md, tests (HF parity for bundled verify), mock MPI

### 21:30 — Align spec protocol + single-graph plan
- Documented user's 5-step iter protocol in plan.md + `target_step.py` module doc
- Renamed `commit_bonus` → `leading_bonus`; bonus never in accept/reject loop
- Phase 8b plan: one S=γ+1 VERIFY graph + `leading_bonus_valid` mask (not two graphs)
- Tests: `had_leading_bonus`, first vs second iter; 9/9 GPU tests pass

### 20:51 — CUDA graph implementation plan
- Reset `cuda_graphs` branch back to `4e9f112` (pre-graph); broken graph commit dropped (recoverable via reflog)
- Audited kernels: all allocate via torch caching allocator only (no raw cudaMalloc/cudaMemcpy/.item) → graph-pool replay should be safe; prior handoff's "rewrite every op allocation-free" likely unnecessary
- Real blocker identified: host-int `write_pos`/`cur_len` baked into graph + host-sliced rope cos/sin
- Wrote `graph_plan.md` (project root): Phases 0–6, DECODE graph first then VERIFY; Phase 0 validates allocation hypothesis before kernel surgery
- **In progress:** plan only, no code yet — awaiting go-ahead to start Phase 0

### 21:05 — CUDA graphs conceptual explainer
- Wrote `documentation/cuda_graphs_explained.md`: launch-bound problem, what a graph is, frozen-argument rules, caching-allocator graph pool
- Part 2 walks the actual decode path (`executor.py`/`buffers.py`/kernels) labeling static vs. must-fix; explains prior 614→3978 replay failure as frozen host-int positions, not allocations
- Maps each concept to `graph_plan.md` phases; no code changes

### 22:15 — test_mpi MPI transfer benchmark
- Added `test_mpi/benchmark.py`: times draft→target (int32[γ] + fp16[γ+1,vocab]) and target→draft (int32[4]) with GPU D2H/H2D
- Defaults: γ=4, vocab=152064 (qwen2.5-7b); uses `protocol.py` Send/Recv helpers + `send_target_result`/`recv_target_result`
- `run_mpi.sh --benchmark --slurm-gpu`; hpcc-gpu-5-2: draft MPI send ~0.19ms (~8 GB/s), full iter ~1.5ms for ~1.45 MiB payload

### 22:25 — MPI benchmark documentation
- Wrote `documentation/mpi_benchmarks.md`: wire payloads, methodology, hpcc-gpu-5-2 results, reproduction commands

### 21:40 — CUDA graph Phase 0: baseline + root cause found
- Added `runtime/benchmarks/phase0_graph_probe.py` (baseline + fixed-pos capture/replay) and `phase0_graph_diag.py` (progressive capture localization)
- Baseline: eager 7B decode = **33.8 tok/s (29.6 ms/token)**, batch=1, Quadro RTX 6000
- Probe: capture → NaN. Diag localized it: rmsnorm "passed" only via an EMPTY graph; one decoder layer diverged at capture and grew across replays (−2.4→−1.6→−34→−47); full stack → NaN
- **Root cause: all custom kernels launch on the default stream (bare `<<<grid,block>>>`, no `getCurrentCUDAStream()`)** → `torch.cuda.graph` records nothing ("CUDA Graph is empty" warning). Allocation theory (handoff) is dead — caching-allocator path is fine (rmsnorm proves it)
- Reordered `graph_plan.md`: new **Phase 1 = stream-correct kernels** (then re-run Phase 0 diag to validate), device scalars → Phase 2, etc. Updated `documentation/cuda_graphs_explained.md` with the stream concept (§1.6) + corrected failure analysis (§2.5)
- Moved `benchmarks/` → `runtime/benchmarks/` (all engine code under runtime/); fixed paths in scripts, `slurm/baseline.slurm`, gpu-cluster SKILL
- SLURM gotchas for the record: init conda hook (`eval "$(conda shell.bash hook)"`) before `source setup.sh`; set `PYTHONNOUSERSITE=1` to avoid `~/.local` torch
- **In progress:** Phase 1 stream fix not started — awaiting go-ahead
